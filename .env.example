# PocketBase Configuration
VITE_POCKETBASE_URL=http://127.0.0.1:8090

# Development settings
VITE_APP_NAME=SiteWise
VITE_APP_ENV=development

# Cloudflare Turnstile Configuration
VITE_TURNSTILE_SITE_KEY=your-turnstile-site-key-here

# =============================================================================
# WebAuthn/Passkey Configuration (Required for PocketBase backend)
# =============================================================================
# These variables should be set in PocketBase environment, not Vite

# URL of the Cloudflare Worker verification service
# For production: https://sitewise-webauthn.your-domain.workers.dev
# WEBAUTHN_VERIFIER_URL=http://localhost:8787

# API key for authenticating with the WebAuthn verifier (shared secret)
# Generate with: openssl rand -base64 32
# WEBAUTHN_VERIFIER_API_KEY=your-32-character-api-key-here

# Relying Party ID (your domain - must match exactly)
# For development: localhost
# For production: yourdomain.com (parent domain covers app.yourdomain.com)
# WEBAUTHN_RP_ID=localhost

# Relying Party display name (shown in browser prompts)
# WEBAUTHN_RP_NAME=Site-Wise

# Comma-separated list of allowed origins for WebAuthn
# Include: production URLs, localhost for dev, tauri:// for desktop app
# WEBAUTHN_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173,tauri://localhost

# 32-character encryption key for storing challenges (REQUIRED)
# Generate with: openssl rand -base64 32 | cut -c1-32
# CRITICAL: Do not use default values - generate a unique key for each environment
# WEBAUTHN_ENCRYPTION_KEY=your-32-character-encryption-key